<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.masroor.cloud_hotel_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AdminMainActivity"
            android:label="Admin" />
        <activity
            android:name=".AddNewRoomActivity"
            android:label="Add New Room" />
        <activity android:name=".ManageRoomsActivity" />
        <activity android:name=".ManageSingleRoomActivity" />
        <activity android:name=".UserMainActivity" />
        <activity android:name=".UserWriteReviewActivity" />
        <activity android:name=".UserBookRoom" />
        <activity android:name=".AdminManageReviews"></activity>
    </application>

</manifest>